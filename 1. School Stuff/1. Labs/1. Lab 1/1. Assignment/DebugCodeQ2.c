/*
This code was given as an assignment for students to identify
and resolve common errors. When done successfully, the code
should compile in its entirity.

Author: Ruán Murgatroyd

C. Date: 04/10/22
*/

#include <stdio.h>

int main()
{
    /* Program to illustrate errors in a C program. */

    int num1;
    int num2;
    float num3;

    num1 = 400;
    num2 = 600;

    printf("The value of num1 is %d", num1);
    printf("\nnum2 is %d", num2);

    return 0;
}